<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <!-- <link rel="stylesheet" href="../../css/style.css"> -->
    <title>Little Maruko</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-transparent">
        <div class="container">
            <a class="navbar-brand" href="/">Little Maruko</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                    <a class="nav-link" href="/teas">Order</a>
                    </li>
                    <% if (currentUser){ %>
                        <li class="nav-item">
                            <a class="nav-link" href="/teas/new">New</a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                    <a class="nav-link" href="/contact/new">Contact</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <!-- Display name if user is sign in -->
                            <% if (currentUser){ %>
                                <%= currentUser.username %> 
                            <% } else { %>
                                User 
                            <% } %>
                        </a>
                        
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <!-- Display sign in, sign out, register accordingly to whether user is signed in -->
                            <% if (!currentUser) { %>
                                <a class="dropdown-item" href="/signin">Sign in</a>
                                <!-- <a class="dropdown-item" href="/register">Register</a> -->
                            <% } else { %>
                                <a class="dropdown-item" href="/signout">Sign out</a> 
                            <% } %> 
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/show-cart"><i class="fas fa-shopping-cart"></i><span class= "badge ml-1 badge-secondary badge-pill" id="cart-counter"><%= session.cart ? session.cart.totalQuantity : 0 %></span></a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>